using UnityEngine;

public class testscript1 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("Integration test successful!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
